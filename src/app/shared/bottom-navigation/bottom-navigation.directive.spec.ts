import { BottomNavigationDirective } from './bottom-navigation.directive';

describe('BottomNavigationDirective', () => {
  it('should create an instance', () => {
    const directive = new BottomNavigationDirective();
    expect(directive).toBeTruthy();
  });
});
